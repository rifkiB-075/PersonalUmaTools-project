{
  "name": "uma-project",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix uma-frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
